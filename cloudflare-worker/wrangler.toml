name = "qpctech-reviews"
main = "index.js"
compatibility_date = "2024-01-01"

[vars]
# GOOGLE_PLACE_ID = ""   # Optional: hard-code after first deploy to skip lookup

# Set secrets via CLI (never commit these):
#   wrangler secret put GOOGLE_PLACES_API_KEY
